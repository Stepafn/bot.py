In the same directory there are bash_script.sh, config.env

1) create a private registry on docker hub
2) run your script ./bash_script.sh
3) docker pull stepafn/tele-bot1.0
4) pull the image that you sent in the script to your private register doker run —env-file config.env stepafn/tele-bot1.0
